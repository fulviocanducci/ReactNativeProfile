import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    textAlign: "center",
    alignItems: "center",
    alignContent: "center",
  },
  text: {
    fontSize: 20,
    fontWeight: "bold",
    marginBottom: 16,
    marginTop: 16,
  },
});
