import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignContent: "center",
    alignItems: "center",
    verticalAlign: "auto",
    marginTop: 15,
    width: "100%",
    borderRadius: 200,
  },
  picture: {
    height: 200,
    width: 200,
    borderRadius: 100,
    resizeMode: "contain",
    borderColor: "#999",
    borderWidth: 1,
  },
});
